import React from 'react';

import { BlockBar } from '../../../';

export default {
  title: 'Components/Organs/BlockBar',
  component: BlockBar
};

const Template = (args) => <BlockBar />;
export const Default = Template.bind({});
